"""DIII-D experimental-data retrieval (MDSplus via mdsthin) and overview/profile
plotting. `mdsthin` is an optional dependency: ``pip install mitim-fusion[mds]``.
Import the submodules directly (kept out of this __init__ so the package imports
without mdsthin installed)::

    from mitim_tools.experiment_tools.diiid import retrieval, plotting
"""
